<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical" >
    <!-- Editext for Search -->

    <ListView
        android:id="@+id/group_list"
        style="@style/AppTheme"
        android:layout_width="fill_parent"
        android:layout_height="360dp"
        android:layout_above="@+id/group_setting"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true" >

    </ListView>

    <Button
        android:id="@+id/group_setting"
        style="@style/AppBaseTheme"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentRight="true"
        android:background="@drawable/btn_selector"
        android:drawableLeft="@drawable/options"
        android:onClick="onConfigureGroupClicked"
        android:padding="10dp"
        android:text="Configure your channel"
        android:textStyle="bold" />

</RelativeLayout> <!-- Editext for Search -->

